#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, t, k, d;
    cin >> n >> t >> k >> d;
    int count = 0;
    int a = ceil(static_cast<double>(n) / k);
    int x = a * t; //time taken by the default

    while( )

}
